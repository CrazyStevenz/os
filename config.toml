[[icedos.repositories]]
url = "github:icedos/apps"
# overrideUrl = "path:/home/stef/code/os/.repos/apps"
modules = [
  "aagl",
  "btop",
  "celluloid",
  "gamemode",
  "gamescope",
  "helium",
  "kitty",
  # "lact",
  "lsfg-vk",
  "mangohud",
  "nautilus",
  "prime-agent",
  "proton-launch",
  # "scx",
  # "solaar",
  "steam-sunshine-headless-session",
  # "steam",
  # "sunshine",
  # "tailscale",
  # "walker",
  "zed",
]

[[icedos.repositories]]
url = "github:icedos/tweaks"
modules = ["gaming", "kernel"]

[icedos.applications.aagl]
launchers = ["anime-game-launcher", "sleepy-launcher"]

# [icedos.applications.protonvpn-cli.connect]
# country = "RO"
# p2p = true

# [icedos.applications.protonvpn-cli.desktop-entry]
# countries = ["RO", "GR", "BG"]

# [icedos.applications.protonvpn-cli.settings]
# anonymousCrashReports = true
# killSwitch = true

# [icedos.applications.scx]
# extraArgs = ["--perf", "1024"]

[icedos.applications.steam]
beta = true
cpuUsageWorkaround = true
downloadsWorkaround = true

[icedos.applications.steam.headless-session]
hdr = true
colorManagement = true
steamOS = true
excludeHostControllers = false

[icedos.applications.sunshine]
autoStart = true
capSysAdmin = true

[icedos.applications.zed]
autosave = true
formatOnSave = true

[[icedos.repositories]]
url = "github:icedos/desktop"

[icedos.desktop]
defaultBrowser = "helium.desktop"
defaultEditor = "dev.zed.Zed.desktop"
keyboardLayouts = [ "us", "gr" ]

[icedos.desktop.clock]
date = true
weekday = true

[[icedos.desktop.entries]]
id = "librewolf" # Firefox privacy flavor
name = "Librewolf"
exec = "icedos pkgs run librewolf"
icon = "librewolf"

[[icedos.desktop.entries]]
id = "mousai" # Song recognizer
name = "Mousai"
exec = "icedos pkgs run mousai"
icon = "mousai"

[[icedos.desktop.entries]]
id = "newsflash" # RSS reader
name = "Newsflash"
exec = "icedos pkgs run newsflash"
icon = "newsflash"

[[icedos.repositories]]
url = "github:icedos/kde"
# overrideUrl = "path:/home/stef/code/os/.repos/kde"
fetchOptionalDependencies = true

[icedos.desktop.kde]
excludeDefaultPackages = ["dolphin", "dolphin-plugins"]

[icedos.desktop.kde.keyboard]
switchingPolicy = "window"

[icedos.desktop.kde.panel]
autohide = true

[icedos.desktop.kde.panel.system-tray]
disabledApplets = [
  "org.kde.plasma.devicenotifier",
  "org.kde.plasma.mediacontroller"
]

[[icedos.desktop.kde.screen-edges]]
position = "topLeft"
action = "none"

[icedos.desktop.kde.shortcuts.hotkeys.kitty]
name = "Kitty"
command = "kitty"
keys = ["Meta+X", "Meta+Χ"]

[icedos.desktop.kde.shortcuts.hotkeys.nautilus]
name = "Nautilus"
command = "nautilus"
keys = ["Meta+E", "Meta+Ε"]

[icedos.desktop.stylix]
disabledTargets = ["blender", "chromium", "console", "kmscon", "vencord", "zed"]

[icedos.desktop.users.stef]
startupScript = "protonvpn-app"

[icedos.desktop.users.stef.idle.lock]
enable = false

[icedos.desktop.users.stef.idle.disable-monitors]
seconds = 300

[icedos.desktop.users.stef.idle.suspend]
enable = false

[icedos.desktop.users.work]
startupScript = "protonvpn-app & slack"

[icedos.desktop.users.work.idle.lock]
seconds = 300

[icedos.desktop.users.work.idle.disable-monitors]
seconds = 270

[icedos.desktop.users.work.idle.suspend]
enable = false

[icedos.desktop.windows]
maximizeButton = true
minimizeButton = true

[icedos.desktop.xdg-desktop-portal]
forceGtkFilePicker = true

[[icedos.repositories]]
url = "github:icedos/hardware"
modules = [
  "bluetooth",
  "disable-ipv6",
  "graphics",
  "kernel",
  "monitors",
  "mounts",
  "pipewire",
  "radeon",
  "ryzen",
  "uinput",
  "upower",
  "zram",
]

[icedos.hardware.graphics.radeon]
featureMask = "0xffffffff"

[icedos.hardware.kernel]
variant = "latest"

[[icedos.hardware.mounts]]
path = "/"
device = "/dev/disk/by-uuid/875ba1fd-ae85-47ec-beac-ec515e776834"
fsType = "btrfs"
flags = ["compress=zstd", "nofail", "x-gvfs-show"]

[[icedos.hardware.mounts]]
path = "/mnt/Nvme"
device = "/dev/disk/by-uuid/ebcec57e-2afb-49a7-8ae8-d6776a841f52"
fsType = "btrfs"
flags = ["compress=zstd", "nofail", "x-gvfs-show"]

[[icedos.hardware.mounts]]
path = "/mnt/SSDGames"
device = "/dev/disk/by-uuid/bb93c901-8df3-4e23-8029-b1816a66ef58"
fsType = "btrfs"
flags = ["compress=zstd", "nofail", "x-gvfs-show"]

[[icedos.hardware.mounts]]
path = "/mnt/HDD"
device = "/dev/disk/by-uuid/a8bdc116-6aac-4f2e-8422-fad546ecf5c0"
fsType = "xfs"
flags = ["nofail", "x-gvfs-show"]

[icedos.hardware.network]
firewall = false

[icedos.hardware.pipewire.echo-cancellation]
enable = true

[icedos.hardware.pipewire.noise-cancellation]
enable = true
# true  = smart filter on the default mic (transparent, follows default source)
# false = separate virtual mic that apps must explicitly select
dynamic = true
# true  = 1-channel rnnoise (mono mics native; stereo mics downmixed to mono)
# false = 2-channel rnnoise (stereo mics preserved; mono mics upmixed, ~2x CPU)
mono = false
# How confident (%) rnnoise must be that a sound is speech before letting it through
vadThreshold = 90
# Ms to keep the gate open after speech stops, preventing abrupt mid-sentence cutoffs
vadGracePeriod = 400
# Ms of audio to retroactively restore when speech is detected, catching clipped first syllables
retroactiveVadGrace = 100

[[icedos.repositories]]
url = "github:icedos/virtualisation"
modules = ["podman"]

[icedos.users.stef]
description = "Stefanos"
packages = [
  # - EMULATORS - #
  # "cemu", # Wiuu
  # "duckstation", # PS1
  # "heroic", # Cross-platform Epic Games Launcher
  # "pcsx2", # PS2
  # "ppsspp", # PSP
  # "prismlauncher", # Minecraft launcher
  # "rpcs3", # PS3

  # - MUSIC - #
  # "eartag", # Music tag editor
  # "flac", # Library and tools for encoding and decoding the FLAC lossless audio file format
  # "nicotine-plus", # p2p music sharing platform
  # "parabolic", # Download web video and audio
  # "spek", # Audio file spectrogram analysis

  # "android-tools", # Required for certain operations on Android phones
  # "audacity", # Sound editor
  # "curtail", # Image compressor
  "faugus-launcher", # Run Windows games using UMU-Launcher
  # "ghex", # HEX editor
  # "nextcloud-client", # Used for two-way syncing with Nextcloud
  "owmods-gui", # Outer Wilds Mod Manager
  # "stremio-linux-shell",
  "qbittorrent",
]

[icedos.users.work]
description = "Work"
packages = [
  "codex",
  "discord",
  "envsubst", # Environment variables substitution for Go
  # "google-cloud-sdk", # Required to use 'gcloud' command
  "jetbrains.datagrip", # Database IDE
  "slack",
  "t3code", # Minimal web GUI for coding agents
  "watchman", # Required to watch when TypeScript files are created/updated/deleted
]

[icedos.system]
generations = 30
packages = [
  "appimage-run",
  "claude-code",
  "gimp3", # GNU Image Manipulation Program
  "gnome-calculator",
  "gsound", # Small library for playing system sounds (required to show file properties in Nautilus)
  "handbrake", # Tool for converting video files and ripping DVDs
  "jetbrains.webstorm", # IDE for Web, JavaScript and Typescript development
  "jq", # Lightweight and flexible command-line JSON processor
  "mission-center", # CPU, Memory, Disk, Network and GPU usage monitor
  "nodejs_22",
  "pnpm", # Fast, disk space efficient package manager for JavaScript
  "proton-vpn",
  "signal-desktop",
  "warp", # Send files via the automatically fastest route
]
permittedInsecurePackages = []
version = "23.05"

[icedos.system.bootloaders.systemd-boot]
mountPoint = "/boot"

[icedos.system.gc]
days = 7 # Number of days before a generation can be deleted
generations = 5 # Number of generations that will always be kept

[icedos.system.git.users.stef]
username = "CrazyStevenz"
email = "github.ekta@aleeas.com"

[icedos.system.git.users.work]
username = "CrazyStevenz"
email = "github.ekta@aleeas.com"

[[icedos.system.overlays.fromChannel]]
url = "github:nixos/nixpkgs/nixos-unstable-small"
packages = ["kdePackages"]

[icedos.system.zsh.aliases]
i = "icedos"
ir = "icedos rebuild"
iu = "icedos rebuild --update"

[[icedos.repositories]]
url = "github:icedos/mcp-server"
# overrideUrl = "path:/home/ice/Projects/icedos/mcp-server"

[home-manager.users.stef.programs.mcp]
enable = true

[home-manager.users.stef.programs.mcp.servers.basic-memory]
command = "nix-shell"
args = [
  "-p",
  "uv",
  "python313",
  "stdenv.cc.cc.lib",
  "gcc",
  "--run",
  "export BASIC_MEMORY_CONFIG_DIR=\"$HOME/.config/basic-memory\"; export BASIC_MEMORY_HOME=\"$HOME/.local/share/basic-memory\"; export BASIC_MEMORY_FORCE_LOCAL=true; export LD_LIBRARY_PATH=$(dirname $(gcc -print-file-name=libstdc++.so.6)):$LD_LIBRARY_PATH; uvx --python 3.13 basic-memory mcp",
]

[home-manager.users.stef.programs.mcp.servers.icedos]
command = "icedos"
args = ["mcp", "serve"]
timeout = 3600000

[home-manager.users.stef.programs.mcp.servers.nixos]
command = "nix-shell"
args = ["-p", "mcp-nixos", "--run", "mcp-nixos"]

[home-manager.users.stef.programs.mcp.servers.chrome-devtools]
command = "nix-shell"
args = ["-p", "nodejs", "--run", "npx -y chrome-devtools-mcp@latest --executable-path=/run/current-system/sw/bin/helium --isolated=true --no-usage-statistics"]
timeout = 600000

[home-manager.users.stef.programs.mcp.servers.context7]
url = "https://mcp.context7.com/mcp"

[icedos.applications.prime-agent]
defaultProvider = "opencode"
defaultModel = "big-pickle"
